{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build",
    "nixpacksPlan": {
      "phases": {
        "install": {
          "cmds": [
            "npm ci --include=dev"
          ]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "node backend/scripts/deploy-migrations.cjs && npm --workspace=backend run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
